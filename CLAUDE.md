# Working notes for AI sessions

The working notes for this repository live in **[`AGENTS.md`](AGENTS.md)** — the single source.
Read that file before doing anything here.

This file exists only so that a Claude Code session landing in this repo picks up the rules
automatically. It is intentionally a pointer: it carries no content of its own, so there is
nothing here to drift out of sync.
